# bgp_elastic_uploader
get download from minio then merge and upload to elasticsearch.


## Build
Use command `make` for build app.

## Usage
set variable in `app.env`

- for crontab app use command `make schedule`